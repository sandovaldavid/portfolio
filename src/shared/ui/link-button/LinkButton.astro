---
import '../button/button.css';

interface Props {
	href: string;
	target?: '_blank' | '_self';
	variant?: 'primary' | 'secondary';
	class?: string;
	/** Screen-reader-only text announcing the link opens in a new tab. Omit for non-navigating targets (e.g. mailto:). */
	newTabLabel?: string;
}

const { href, target = '_blank', variant = 'primary', class: className, newTabLabel } = Astro.props;
---

<a
	{href}
	{target}
	rel={target === '_blank' ? 'noopener noreferrer' : undefined}
	role="link"
	class:list={[
		'btn-base',
		variant === 'primary' ? 'btn-primary' : 'btn-secondary',
		'group',
		className,
	]}
>
	<slot />
	{target === '_blank' && newTabLabel && <span class="sr-only"> {newTabLabel}</span>}
</a>
