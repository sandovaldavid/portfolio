---
interface Props {
	eyebrow?: string;
	title: string;
	emphasis?: boolean;
}

const { eyebrow, title, emphasis = false } = Astro.props;
---

<article
	class:list={[
		'group flex h-full min-w-0 flex-col gap-3 bg-channel-surface-default p-4 transition-[transform,box-shadow,border-color] duration-150 hover:-translate-y-0.5 hover:border-edge-strong hover:shadow-retro-md motion-reduce:transform-none motion-reduce:transition-none md:p-5',
		emphasis ? 'border-2 border-edge-strong shadow-retro-md' : 'border border-edge-subtle',
	]}
	data-experience-panel
>
	{
		eyebrow && (
			<p class="text-editorial-label-compact uppercase text-channel-accent-primary">
				{eyebrow}
			</p>
		)
	}
	<h3 class="text-component-title text-content-strong">{title}</h3>
	<div class="text-secondary text-content-default md:text-body [&>p+p]:mt-3">
		<slot />
	</div>
</article>
