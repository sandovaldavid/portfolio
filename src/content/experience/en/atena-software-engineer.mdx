---
experienceId: atena-software-engineer
locale: en
company: Atena
title: Jr. Software Engineer
dateLabel: Jan 2026 – Present
summary: Professional fintech work on existing .NET and Angular systems, with a backend-oriented focus on correctness, authorization, service integration, debugging and validation.
achievements:
    - Investigate bugs by tracing assumptions through affected callers, data boundaries and system behavior instead of treating the first symptom as the whole problem
    - Harden authorization and tenant-scoped flows toward explicit fail-closed behavior, including consumer impact when contracts change across service boundaries
    - Use TypeScript strictness, compiler feedback, static analysis and layered debugging to locate affected behavior across frontend and backend code
    - Validate fixes with targeted regression checks and revise my own conclusion when new evidence shows that the original explanation was incomplete
---

<ExperienceSection
	eyebrow="WORKING ON EXISTING SYSTEMS"
	title="Correctness starts with the assumptions behind the behavior"
	layout="split"
>
	<ExperiencePanel
		eyebrow="DEBUGGING + CONTRACTS"
		title="Trace the rule before changing the code"
		emphasis
	>
		When a bug appears, I try to follow the underlying assumption through its callers and system
		boundaries before choosing the fix. If the real state is ambiguous, I prefer an explicit
		contract or failure over a deterministic fallback that can still return the wrong answer.
	</ExperiencePanel>
	<ExperiencePanel
		eyebrow="AUTHORIZATION + INTEGRATION"
		title="Boundaries are part of the feature"
	>
		I work across backend and frontend concerns, including authorization, tenant scoping and
		contract changes that can span services. Fail-closed behavior and affected consumers are
		part of the change itself, not a check added after implementation.
	</ExperiencePanel>
</ExperienceSection>

<ExperienceSection
	eyebrow="VALIDATION"
	title="I try to make the fix falsifiable"
	surface="highlight"
>
	Tests, compiler feedback and static analysis are tools I use to challenge a change, not just to
	make a pipeline green. For regression-prone behavior, I check that the relevant test would fail
	again if the fix were removed. That same discipline means changing my own conclusion when new
	evidence shows that my first explanation was wrong.
</ExperienceSection>
