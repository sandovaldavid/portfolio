---
experienceId: atena-software-engineer
locale: en
company: Atena
title: Jr. Software Engineer
dateLabel: Jan 2026 – Present
summary: Professional fintech work on existing software, with a backend-oriented focus on correctness, authorization, cross-system contracts, debugging and validation alongside hands-on frontend delivery.
achievements:
    - Traced defects beyond reported symptoms by following changed assumptions through affected consumers and rejecting plausible defaults when ambiguity could hide incorrect behavior
    - Worked on tenant-scoped authorization and fail-closed semantics, distinguishing absent restrictions from an explicitly empty allowed set and rechecking enforcement boundaries
    - Implemented changes that crossed backend, frontend and service contracts, checking affected consumers and deployment order when compatibility depended on sequencing
    - Used TypeScript strictness, static analysis and targeted regression tests to surface affected consumers and verify changed behavior
---

<ExperienceSection
	eyebrow="HOW I WORK"
	title="Existing software changes the debugging problem"
	layout="split"
>
	<ExperiencePanel
		eyebrow="CORRECTNESS"
		title="Trace the assumption, not only the symptom"
		emphasis
	>
		When a domain assumption changes, I trace the callers and consumers that relied on the old
		behavior. A deterministic default is not automatically a safe answer; if the domain is
		ambiguous, I prefer making that ambiguity explicit rather than returning something plausible
		but wrong.
	</ExperiencePanel>
	<ExperiencePanel eyebrow="BOUNDARIES" title="Authorization and contracts need explicit semantics">
		I work across tenant-scoped authorization and integration boundaries where “no restriction”
		and “an explicitly empty allowed set” cannot mean the same thing. Cross-service changes also
		need their consumers and deployment sequence checked when compatibility spans repositories.
	</ExperiencePanel>
</ExperienceSection>

<ExperienceSection
	eyebrow="VALIDATION"
	title="A useful check should be able to prove the fix wrong"
	surface="highlight"
>
	I use compiler and type-system feedback, static analysis and targeted tests as ways to find the
	blast radius of a change, not as decorative gates. When a regression matters, I want the test to
	fail again if the old behavior is reintroduced. And when new evidence changes my diagnosis, I
	update the conclusion rather than defend the first explanation.
</ExperienceSection>
